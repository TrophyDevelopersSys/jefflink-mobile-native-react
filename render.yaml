# Single render.yaml at repo root — Render auto-detects this file.
# Both services build from workspace root so Turbo caching works across packages.
# Mobile (Expo) is NOT deployed here — use EAS Build instead.
#
# CURRENT DNS (Cloudflare):
#   CNAME  www  → jefflink.onrender.com       ← update to jefflink-web.onrender.com  after deploy
#   CNAME  api  → jefflink.onrender.com       ← update to jefflink-api.onrender.com  after deploy
#   CNAME  cdn  → pub-ac2067b6a2264561b99c6c807174ff78.r2.dev  (Proxied — no change needed)

services:

  # ─── API Service (NestJS) ─────────────────────────────────────────────────
  # After deploy: update Cloudflare CNAME api → jefflink-api.onrender.com
  - type: web
    name: jefflink-api
    env: node
    plan: starter
    rootDir: .
    buildCommand: "pnpm install --frozen-lockfile && pnpm turbo run build --filter=jefflink-backend"
    startCommand: node apps/backend/dist/main.js
    healthCheckPath: /health
    envVars:
      - key: NODE_ENV
        value: production
      - key: DATABASE_URL
        sync: false
      - key: JWT_SECRET
        sync: false
      - key: JWT_EXPIRES_IN
        value: 7d
      - key: REDIS_URL
        sync: false
      - key: CORS_ORIGINS
        value: https://jefflinkcars.com,https://www.jefflinkcars.com
      - key: R2_ACCESS_KEY
        sync: false
      - key: R2_SECRET_KEY
        sync: false
      - key: R2_BUCKET_NAME
        sync: false
      - key: CDN_BASE_URL
        value: https://cdn.jefflinkcars.com

  # ─── Web Service (Next.js 15) ──────────────────────────────────────────────
  # After deploy: update Cloudflare CNAME www → jefflink-web.onrender.com
  - type: web
    name: jefflink-web
    env: node
    plan: starter
    rootDir: .
    buildCommand: "pnpm install --frozen-lockfile && pnpm turbo run build --filter=@jefflink/web"
    startCommand: pnpm --filter @jefflink/web start
    envVars:
      - key: NODE_ENV
        value: production
      - key: NEXT_PUBLIC_API_BASE_URL
        value: https://api.jefflinkcars.com/api/v1
      - key: NEXT_PUBLIC_SITE_URL
        value: https://jefflinkcars.com
      - key: NEXT_PUBLIC_CDN_URL
        value: https://cdn.jefflinkcars.com
