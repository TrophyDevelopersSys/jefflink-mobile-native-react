# NOTE: Render is configured via /render.yaml at the repo root.
# Do not add service definitions here — Render should only read one render.yaml.
# Keeping this file as a placeholder to avoid accidental service creation.
