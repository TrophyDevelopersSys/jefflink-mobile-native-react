# NOTE: The Expo / React Native mobile app is NOT deployed to Render.
# Builds are handled via EAS Build (eas build --platform android|ios).
# This file is intentionally left as a placeholder — do not configure
# a Render service pointing to apps/mobile.
