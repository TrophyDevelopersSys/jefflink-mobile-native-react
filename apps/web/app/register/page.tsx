import type { Metadata } from "next";

export const metadata: Metadata = {
  title: "Create Account",
  description: "Join JeffLink — Uganda's leading hire-purchase and marketplace platform.",
};

export { default } from "./RegisterForm";
